1、所有服务器配置使用的字符串改为 二进制。X
2、服务器内存中的字符串全部改为二进制类型处理。X
3、一种潜在的优化:将多个玩家进程合并为一个玩家进程。X
4、网关的do_parse_packet有效率问题，需要将{packet,2}加到socket参数中.这个等周星佑有空改一下。X
5、战斗接口代码未完善，最初设计的战斗运算节点，到底做不做？X
6、玩家无脑连续点挑战，或者前一次挑战未完成，就开始请求下一次挑战。解决方法：增加战斗请求锁。X
7、proto:encode生成代码，使用协议模块号做匹配X
8、在网关进程做一个统一的验证请求包速度的功能，很重要！！！！
9、定期清理邮件与战报的脚本未做！！
10、武将结构调整，跟道具类似，做起来简单点。然后战斗用另外的属性。





已完成的优化：
1、武将数据精简，未上阵武将理解为道具，只有基础信息。上阵武将才参与到各系统运作，具有完整属性。 gerSimple+ger。  
2、道具数据精简，背包中的装备和非装备道具，只具有基础信息。装在身上的装备才具有完整属性。
3、向客户端发协议的接口，改为了直接发送，删除了代理转发的步骤。
4、协议打包模块使用iolist()代替binary(),使性能提高了一倍，可喜可贺！！。之前做过的协议打包优化是,生成展开后的静态代码，减小函数调用次数，完成了一次性能提升一倍的CHEER！


其他想法：
1、商店，商品，公告等也使用ets，有利于动态更新。或者写个转换，插入到数据库。
2、对所有的排行榜，使用一个server单独管理，当排行榜更新或者其他动态计算时，不会影响每个server的运行。
3、再做一个广播server，主要处理boss等server中的广播消息，减小各个游戏server的负载。
4、对每个玩家不单独创建进程，而是将玩家信息添加到ets中。应该就是上面优化3要做的事情。改动比较大。